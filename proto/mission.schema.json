{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/genesis-ssmp/genesis/proto/mission.schema.json",
  "title": "Mission",
  "description": "A high-level mission assigned by God to be decomposed into tasks for Angels.",
  "type": "object",
  "required": ["mission_id", "base_rev", "goal", "leases", "tasks", "token_budget", "created_at"],
  "properties": {
    "mission_id": {
      "type": "string",
      "description": "Unique identifier for this mission."
    },
    "base_rev": {
      "type": "string",
      "description": "Git commit hash or content-address of the codebase revision this mission targets."
    },
    "goal": {
      "type": "string",
      "description": "Natural-language description of the mission objective."
    },
    "leases": {
      "type": "array",
      "description": "Leases granted for this mission.",
      "items": {
        "type": "string"
      }
    },
    "tasks": {
      "type": "array",
      "description": "Ordered list of task IDs comprising this mission.",
      "items": {
        "type": "string"
      }
    },
    "token_budget": {
      "type": "integer",
      "minimum": 0,
      "description": "Maximum token spend authorized for this mission."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of mission creation."
    }
  },
  "additionalProperties": false
}
